#ifndef ASC_STL_INCLUDE_ASC_STD___HOST_STDLIB_ISTREAM_
#define ASC_STL_INCLUDE_ASC_STD___HOST_STDLIB_ISTREAM_

#include "asc/std/__config"

#if defined(__CCE__)
#define ASC_DEVICE_CODE
#else
#include <istream>
#endif

// [MIGRATION NOTE] The original CCCL header conditionally included the host's
// <istream> via _CCCL_HOSTED(). ASC-STL does not have _CCCL_HOSTED, so we
// include <istream> directly on the host side. On the device side, <istream>
// is not available.

#endif // ASC_STL_INCLUDE_ASC_STD___HOST_STDLIB_ISTREAM_
