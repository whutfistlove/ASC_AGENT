{
  "input_init": "h_in0[i] = static_cast<float>(i % 97) - 40.0f; h_in1[i] = static_cast<float>((i * 3) % 97) - 50.0f;",
  "element_op_code": "auto pr = ascend::std::minmax(in0_val, in1_val); out0_val = pr.first; out1_val = pr.second;",
  "golden_code": "expected0 = (in1_ref < in0_ref) ? in1_ref : in0_ref; expected1 = (in0_ref < in1_ref) ? in1_ref : in0_ref;",
  "gm_inputs": 2,
  "gm_outputs": 2,
  "dtype": "float"
}