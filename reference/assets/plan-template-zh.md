# <operator_name> 迁移执行计划

## 1. 目标与范围

- 算子名称：
- 原始 CUDA 文件位置：
- 迁移模式：
- 源产物形态：
- 目标迁移模式：
- 目标交付形态：
- 实际输出模式：
- 迁移目标：
- 若实际输出形态与源产物不一致，形态差异说明：
- 若存在真实降级，降级原因：
- 当前阻塞：
- 恢复到目标模式所需条件：
- 一比一复刻目标：
- 本次迁移范围：
- 本次不包含内容：
- 原生 JIT 路径处理结论：
- complex 类型路径处理结论：
- double 设备侧路径处理结论：

## 2. CUDA 函数清单

| 类型 | 所属结构体 | 函数/方法名 | 文件 | 说明 |
|------|------|------|------|------|

## 3. 调用链分析

### 3.1 <kernel_name> 调用链

```text
<host entry>
└── <kernel_name>
    └── <device callee>
```

## 4. API 映射分析

### 4.1 <kernel_name> API 映射

| API 类型 | CUDA API | Ascend API | 迁移方式 | 说明 |
|------|------|------|------|------|

## 5. 依赖闭包分析

| 依赖项 | 依赖类型 | 来源 | 处理方式 | 说明 |
|------|------|------|------|------|

依赖类型填写建议：

- 公共上层抽象
- CUDA 特有依赖
- 其他

处理方式填写建议：

- `reuse`
- `migrate`
- `downgrade`
- `blocked`
- `excluded`

## 6. 能力覆盖矩阵

| 能力项 | 源能力说明 | 当前处理 | 影响范围 | 恢复条件/备注 |
|------|------|------|------|------|

能力项至少覆盖：

- dtype 覆盖
- complex dtype 路径
- double 设备侧路径
- 行为分支覆盖
- layout / stride 假设
- shape / size fast path
- kernel 选择行为
- 调用路径
- 原生 JIT 路径
- 测试路径

当前处理填写建议：

- `preserve`
- `reuse`
- `migrate`
- `downgrade`
- `blocked`
- `excluded`

## 7. 函数迁移方案

### 7.1 <function_name> 迁移方案

- 原始职责：
- 迁移方式：
- 是否直接映射：
- 需要重写的逻辑：
- 语法替换点：
- 语法/约束依据：
- 风险说明：

### 7.2 不支持路径处理

- 原生 JIT 相关路径是否存在：
- 原生 JIT 相关源文件 / 函数 / 调用入口：
- 原生 JIT 路径处理结论：`remove_and_record` / `blocked` / `not_applicable`
- 原生 JIT 路径处理依据：
- 是否仅为 PyTorch dispatcher fake/meta/torch.compile 兼容注册而非原生运行时编译：
- `plan.md` 中的影响说明：
- complex 类型路径是否存在：
- complex 类型涉及的源文件 / 函数 / dtype 分支：
- complex 类型处理结论：`remove_and_record` / `blocked` / `not_applicable`
- complex 类型处理依据：
- 对 torch 调用路径、注册逻辑和验证范围的影响：
- double 设备侧路径是否存在：
- double 涉及的源文件 / 函数 / dtype 分支 / device helper / FP64 intrinsic / 验证用例：
- double 设备侧路径处理结论：`remove_and_record` / `blocked` / `not_applicable`
- double 设备侧路径处理依据：
- 对调用路径、注册逻辑和验证范围的影响：

### 7.3 可复用抽象 / Ascend Counterpart 方案

- 候选抽象层：
- 来源文件：
- 是否可被兄弟算子复用：
- 本次决策：`direct include` / `ascend counterpart` / `downgrade` / `blocked` / `excluded`
- 决策原因：
- 若不实现 counterpart，对当前与后续算子的影响：

## 8. 迁移实施顺序

1. 
2. 
3. 

## 9. 测试优先策略

- 验证入口类型：
- 目标行为：
- 对应验证方式：
- 计划复用的现有测试：
- 计划新增的测试：
- 预期失败或预期缺口：
- 通过标准：

验证入口类型填写建议：

- `main`
- `UT/ST`
- `gtest`
- `Python`
- `import`
- `package`

## 10. 风险与阻塞

### 10.0 降级决策门

- 是否存在重大降级候选：
- `out-of-tree` 是否仅为默认允许路径且不单独触发审批：
- 若存在，候选项：
- 是否涉及抽象层未一比一复刻：
- 是否涉及多路径 kernel 调度被简化为单路径：
- 与用户沟通前是否暂停编码：
- 在获得用户明确批准前是否禁止编码：
- 是否已触发硬停止审批门：
- 用户是否已明确批准：
- 备选方案对比：
  - 方案 A：
  - 方案 B：
- 对比维度：
  - 迁移成本：
  - 迁移难度：
  - 迁移收益：
  - 影响算子范围：
  - 验证影响：
  - 恢复路径：
- 用户最终选择：

硬规则：

- 若“是否已触发硬停止审批门”为“是”，则在“用户是否已明确批准”为“是”之前，所有实现相关状态必须保持 `pending`
- 在这种场景下，记录到 `plan.md` 或 `README.md` 不能替代用户批准
- `out-of-tree` 若是仓库默认支持路径，只能记录为工程形态说明，不能单独作为重大降级候选
- 若仅存在 `out-of-tree` 形态差异，则应在第 1 节填写“形态差异说明”，不要写成“降级原因”

### 10.1 已识别风险

- 
- 原生 JIT 路径删除后的调用路径差异：
- complex 类型不支持带来的 dtype 覆盖差异：
- double 设备侧路径排除后的 dtype / 行为覆盖差异：

### 10.2 已识别阻塞

- 

### 10.3 阻塞处理建议

- 建议动作：
- 所需条件：
- 下一步责任：

### 10.4 根因分析记录

- 问题现象：
- 初步根因判断：
- 验证方法：
- 处理结论：

## 11. 验证计划

### 11.1 环境检查

- 设备检查：
- 环境变量检查：
- 编译工具检查：

### 11.2 原生/C++ 侧验证计划

- 验证对象：
- 验证入口：
- 测试方法：
- 首次验证结果：
- 通过标准：

### 11.3 Python 侧验证计划

- 是否需要：
- 验证入口：
- 验证方法：
- 首次验证结果：
- 通过标准：

### 11.4 出包/安装验证计划

- 是否需要：
- 验证入口：
- 验证方法：
- 首次验证结果：
- 通过标准：

## 12. 迁移进展

| 项目 | 状态 | 说明 |
|------|------|------|

## 13. 执行记录与证据

- 环境初始化命令：
- 编译命令：
- 原生/C++ 侧验证命令：
- Python 侧验证命令：
- 出包/安装命令：
- 关键语法/约束依据：
- 关键结果摘要：

## 14. 最终验证结论

- Ascend 950 PR 实机验证状态：
- 构建状态：
- 原生/C++ 侧验证状态：
- Python 侧验证状态：
- 出包/安装状态：
- 结果证据：
- 失败/阻塞根因摘要：
- 最终结论：

最终结论必须使用 `reference/validation-checklist.md` 中的最终状态标签之一。
