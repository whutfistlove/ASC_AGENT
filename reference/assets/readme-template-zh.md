# <operator_name> 迁移说明

## 1. 算子说明

- 算子名称：
- 迁移模式：
- 来源与交付形态摘要：
- 功能概述：
- 输入输出说明：
- 一比一复刻结论：

## 2. 原始 CUDA 实现摘要

- 原始实现文件：
- Kernel 入口：
- 用户侧调用路径摘要：

## 3. Ascend C SIMT 迁移摘要

### 3.1 迁移策略

- 
- 是否实现可复用 Ascend counterpart：

### 3.2 主要改动与结论

- 
- 重大降级决策（如有）：

### 3.3 API 与语法替换说明

| CUDA 项 | Ascend 项 | 说明 |
|------|------|------|

### 3.4 依赖与能力覆盖结论

- 依赖闭包处理：
- 能力覆盖结论：
- shape / kernel 选择行为保留情况：
- 语法规避依据：
- 显式降级项：

## 4. 目录结构

按实际迁移模式填写目录结构。以下仅为占位示意，不要求所有模式完全一致。

```text
ported-ops/<operator_name>/
├── plan.md
├── <mode-specific files>
├── README.md
└── <tests or validation assets>
```

## 5. 构建方式

### 5.1 环境准备

```bash
source /usr/local/Ascend/cann/set_env.sh
```

### 5.2 编译步骤

```bash
# 按实际迁移模式填写
```

### 5.3 安装步骤（按需）

```bash
# `torch_npu` / `pybind` 等需要安装时填写
```

### 5.4 出包步骤（按需）

```bash
# 如需出包，单独填写，不要与测试命令混写
```

## 6. 验证与使用方式

### 6.1 原生/C++ 侧验证

```bash
# `sample` 可直接复用 main；也可填写 UT/ST 或 gtest
```

### 6.2 Python 侧验证（按需）

```bash
# `torch_npu` / `pybind` 等需要 Python 调用时填写
```

### 6.3 出包验证（按需）

```bash
# 如需验证包产物，在此填写
```

## 7. 最终验证结果摘要

- 验证环境：
- Ascend 950 PR 实机验证：
- 构建结果：
- 原生/C++ 侧验证结果：
- Python 侧验证结果：
- 出包/安装结果：
- 结果校验结论：
- 当前状态标签：

当前状态标签必须使用 `reference/validation-checklist.md` 中的最终状态标签之一。

## 8. 已知限制

- 显式降级项：
- 显式排除项：
- 
- 已阻塞项：
- 用户已接受的降级项：

## 9. 问题排查记录

- 问题阶段：
- 问题现象：
- 根因判断：
- 已验证动作：
- 相关语法/约束依据：

## 10. 后续建议

- 

## 11. 未完成事项

- 
