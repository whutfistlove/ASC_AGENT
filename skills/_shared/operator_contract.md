最高原则：**CCCL/ACCL 算子的语义是基准（ground truth）。测试必须适配算子的真实形态，绝不能为了凑测试模板或让测试通过而改变算子的签名、返回类型或语义。** 例如 `swap` 是 `void swap(T&,T&)`（原地交换），就必须按原地交换来测，绝不能写成 `auto out = swap(a, b)`。

当失败根因在测试本身时（例如把 `void`/原地算子当成"二元返回值"来用、把右值绑定到非 const 左值引用、把 `void` 赋给 `float`、参数个数/类型不符），应**修复测试**让它适配算子真实形态，而不是反过来改算子。
